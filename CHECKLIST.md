# Чекліст

`[x]` ставиться лише після реальної перевірки — запущено і працює.
Правила ведення — у [CLAUDE.md](CLAUDE.md).

## Зроблено

- [x] Скафолд `frontend/` (React 19 + TypeScript + Vite)
- [x] Скафолд `backend/` (ASP.NET Core 8 Web API, minimal API)
- [x] `Backend.sln`
- [x] `strict: true` у `tsconfig.app.json`
- [x] `.gitignore` для бекенду (`bin`, `obj`, `data`, налаштування IDE)
- [x] Vite proxy `/api` → `localhost:5244` замість CORS: браузер бачить одну адресу
      і в dev, і в проді. `AddCors`/`UseCors` і регулярку LAN-origin прибрано,
      `VITE_API_BASE_URL` більше не потрібен — шляхи відносні. Перевірено реальним
      завантаженням файлу через `localhost:5173/api/notes/upload` → 200
- [x] `POST /api/notes/upload` — заглушка: зберігає файл у `data/assets` під GUID,
      повертає метадані. Без AI-обробки. Переїхав на `POST /api/assets` (див. нижче)
- [x] Форма завантаження файлу на фронтенді + показ відповіді
- [x] CRUD над завантаженими файлами: новий зріз `Controllers/Assets/` і `/api/assets` —
      `POST` (створення, `201` + `Location`), `GET` (список), `GET /{fileName}` (байти),
      `DELETE /{fileName}`. Upload переїхав сюди з `/api/notes/upload`: файли — окрема
      сутність, а не підрозділ нотаток, яких ще нема взагалі. `NotesController`
      прибрано — після переїзду в ньому не лишилось жодної дії.
      «U» з CRUD немає свідомо: замінити байти = завантажити наново і отримати нове ім'я.
      Адреса файлу — збережене ім'я (`{guid}.pdf`), а не `id` окремо: тільки повне ім'я
      вказує на конкретний об'єкт і на диску, і в бакеті.
      Ім'я з маршруту йде у шлях до файлу, тож `AssetFileName.IsSafe` відбиває все, що
      здатне вилізти з теки; перевірка стоїть у самому сховищі, а не лише в контролері.
      Content-Type при віддачі вгадується з розширення (`FileExtensionContentTypeProvider`).
      Перевірено curl'ом на **обох** провайдерах — `Local` і `S3` (Garage):
      login → список → upload (`201`, `Location`) → скачування (байти ті самі) →
      `DELETE` (`204`) → повторний `DELETE` (`404`) → скачування після видалення (`404`);
      неіснуюче ім'я `404`, `..%2F..%2Fappsettings.json` і `%2E%2E%5C...` → `404`,
      без cookie `401`, порожній файл `400`. Через Vite proxy (`localhost:5173`) —
      теж `201`. Тека `data/assets` після прогону чиста
- [x] Фіча-флаги: секція `Features` у конфізі → `FeatureOptions`, `GET /api/features`
      для фронту (анонімний — екран логіну має знати стан до входу).
      Перший прапорець — `Features:UploadEnabled`, зараз `false`: завантаження
      вимкнено. Заборона стоїть **в `AssetsController.Upload`** (`403`), а не тільки
      в UI — заблокована кнопка є підказкою, а не обмеженням; curl'ом її нема.
      Інжектиться `IOptionsSnapshot<FeatureOptions>`, а не `IOptions<>`: перший
      перечитується на кожен запит, тож прапорець перемикається правкою
      `appsettings.json` без перезапуску. На Render — `Features__UploadEnabled`.
      Фронт до відповіді `/api/features` вважає фічу **вимкненою**: показати робочу
      кнопку, яку сервер потім відкине, гірше, ніж на мить показати заблоковану.
      `readOnly` на `<input type="file">` браузер ігнорує, тому саме `disabled`.
      Другий прапорець — `Features:DownloadEnabled` (зараз `true`): гейтить
      `GET /api/assets/{fileName}`, і при `false` ім'я файлу в списку рендериться
      текстом замість `<a href>`. Два напрямки — два прапорці: upload це від
      користувача на сервер, download — назад до нього
- [x] Список завантажених файлів на фронтенді: `AssetList.tsx` — таблиця (ім'я, розмір,
      дата), посилання на скачування, кнопка видалення з `confirm`.
      Скачування — звичайний `<a href>`, не `fetch`: той самий origin, тож cookie
      піде сама, а браузер сам віддасть файл. Через `fetch` довелося б збирати Blob
      і клікати синтетичне посилання.
      Форма і список — сусіди, тому спільний стан піднято в `App`: лічильник
      завантажень, який форма інкрементує після успіху, а список тримає в залежностях
      ефекту. Після видалення рядок прибирається з локального стану, без повторного
      `GET`. Перезавантаження списку навмисне лишає старі рядки на екрані замість
      «Loading…» — інакше таблиця блимала б після кожного завантаження.
      Перевірено в браузері (субагент, dev-сервери): вхід → таблиця з двома наявними
      файлами, розміри `78.4 KB`/`321.4 KB`, новіші зверху → upload `201` і рядок
      з'явився сам, без перезавантаження сторінки → клік по імені `GET` `200`
      з `Content-Disposition: attachment` → `confirm` показує ім'я файлу →
      `DELETE` `204` і рядок зник без перезавантаження → скасування `confirm`
      не шле запиту взагалі → після `F5` список рендериться, сесія жива
- [x] Структура бекенду замість одного `Program.cs` на 213 рядків — **вертикальні зрізи**:
      `Controllers/<фіча>/` містить сам контролер у корені теки і підтеки з допоміжними
      файлами (`Contracts/`, `Services/`, `Configuration/`, згодом `Validators/`).
      Спільне, що не належить жодному контролеру, — в `Infrastructure/Storage/`
      і `Infrastructure/Hosting/`. Щоб змінити логін, відкривається одна тека, разом із
      реєстрацією в DI. `Program.cs` — 30 рядків, список фіч.
      Стан переїхав із замикань у DI, тож у тестах його можна буде підмінити.
      Проміжний варіант із поділом за шаром на верхньому рівні (`Endpoints/` +
      `Contracts/` + `Services/`) відкинуто: на 30 ендпоінтах кожна фіча була б
      розмазана по п'яти теках. Ім'я `Controllers/` (а не `Features/`) обране свідомо —
      як у робочому проєкті користувача; ASP.NET на розташування контролерів не дивиться,
      теки тут суто для людей
- [x] MVC-контролери замість minimal API (рішення від 2026-09-06): `AuthController`,
      `NotesController` з `[ApiController]`, `[Route("api/[controller]")]`, `[Authorize]`
      на класі й `[AllowAnonymous]` на `login`. Причина — той самий стиль, що на роботі
      користувача: одні звички читаються в обидва боки
- [x] Поведінка після переїзду не змінилась; перевірено тим самим набором curl: `me`/`upload`
      без cookie → 401, невірний пароль → 401, вірний → 200 + `Set-Cookie` HttpOnly,
      `me`/`upload` з cookie → 200, порожній файл → 400, logout → 204 і далі `me` → 401,
      5 невдалих спроб → 429 (вірний пароль при вичерпаному ліміті теж 429),
      `/health` → 200, `/api/nope` → 404, `/swagger` у Development → 200.
      Шляхи, які кличе фронт, не змінились — правок у `frontend/` не потрібно
- [x] Документація API у Swagger: `GenerateDocumentationFile` + `IncludeXmlComments`,
      `///` на контролерах і діях, `[ProducesResponseType]` з типами.
      Перевірено по згенерованому `/swagger/v1/swagger.json`: описи методів, параметрів
      і кожного статусу на місці, тіла відповідей — з типами (`CurrentUserResponse`,
      `UploadedAssetResponse`). `NoWarn 1591` — щоб компілятор не вимагав `///`
      на кожному публічному типі, вони потрібні лише на ендпоінтах
- [x] `LowercaseUrls = true`: токен `[controller]` бере ім'я класу як є, і в схему
      потрапляло `/api/Auth/login`. Маршрутизація регістронечутлива, тож виклики
      працювали, але згенерований із схеми клієнт тягнув би за собою те написання.
      Перевірено по `swagger.json`: шляхи `/api/auth/...`, `/api/notes/upload`
- [x] Пастка minimal API (лишається на пам'ять, якщо колись повернемось): хендлер, у якого
      єдиний параметр — `HttpContext`, підходить під перевантаження `MapPost` з
      `RequestDelegate`, і повернутий `IResult` мовчки викидається — `logout` віддавав би
      200 з порожнім тілом замість 204. Ловиться аналізатором `ASP0016`, лікується
      приведенням `(Delegate)`. У контролерах цієї пастки немає взагалі
- [x] Оновлення сторінки в реальному часі через **SSE** (Server-Sent Events): завантажив
      з телефона — рядок з'явився у вкладці на ПК. Не WebSocket: канал потрібен
      односторонній, а SSE — це звичайний GET, якому сервер просто не дописує відповідь,
      і `EventSource` сам перепідключається після обриву.
      Механізм зроблено спільним, не під список файлів: `Infrastructure/RealTime/` —
      `IChangeNotifier` (синглтон) роздає `ChangeEvent(Resource, Action, Id)` по підписках,
      кожна зі своїм каналом `Channel.CreateBounded(32, DropOldest)`, щоб клієнт, який
      перестав читати, не підвісив чужий upload. Стрім віддає `Controllers/Events` —
      `GET /api/events`, `[Authorize]`, `: ping` раз на 20 с (тримає проксі від таймауту
      **і** виявляє мертвий сокет: запис падає й цикл виходить по `CancellationToken`).
      На фронті — `api/realtime.ts` (одне з'єднання на вкладку з підрахунком підписників)
      і хук `useResourceChanges('assets', reload)`: наступному компоненту потрібен рядок.
      **Подія — сигнал, а не дані:** журналу подій немає, тож проґавлене не відтворити;
      клієнт після кожного (пере)підключення просто перечитує колекцію. Тому втрачена
      подія нешкідлива.
      Перевірено `curl`'ом **через Vite-проксі** (там ризик буферизації): upload → `data:`
      у ту саму секунду, delete → друга подія, `: ping` рівно через 20 с, невдалий
      `DELETE` (404) події не шле. Віяльна розсилка — двома одночасними стрімами
      (5173 і 5244): обидва отримали обидві події
- [x] З'єднання не висить безкінечно: воно живе, лише поки є підписник, вкладка видима
      (`visibilitychange` із паузою 30 с, щоб Alt-Tab не рвав стрім) і була активність
      за 15 хвилин. Не `window.blur`: вікно на другому моніторі видно, і глушити його
      оновлення неправильно. Перепідключення з backoff 2→60 с — браузер сам відновлює
      обірвану мережу, але після HTTP-помилки (502 при рестарті API, протухла сесія)
      здається назавжди
- [x] Автоматичні перезапити зламали б `AssetList`, тож заразом: фонове оновлення, яке
      впало, більше **не стирає список** (раніше будь-який `catch` гасив рядки — при
      перезапитах без запиту користувача це неминуче), і лічильник запитів замість
      прапорця `cancelled` — подія може прилетіти під час попереднього запиту, і старша
      відповідь не має перетерти новішу

## Найближче

- [ ] Валідація вхідних моделей (FluentValidation у `Controllers/<фіча>/Validators/`, як у
      робочому проєкті). Зараз валідувати нічого — `LoginRequest` має одне поле, і
      порівняння пароля саме собі перевірка. Актуально, коли з'явиться створення нотатки
- [ ] Модель зберігання нотатки: YAML-frontmatter у `.md` (заголовок, категорія, дата, теги)
- [ ] `GET /api/notes` — список нотаток
- [ ] `GET /api/notes/{id}` — вміст однієї нотатки
- [ ] Перегляд списку нотаток на фронтенді
- [ ] Рендер `.md` у HTML для перегляду

## Автентифікація

Рішення від 2026-09-06: скасовує попереднє «автентифікації немає і не планується».
Це **передумова деплою** — без неї публічний URL означає відкритий доступ до нотаток
і анонімний upload. Робиться першою, до інфраструктури нижче.

- [x] Механізм обрано: cookie-сесія, пароль з конфігу. Джерело identity відчеплене
      від сесії — GitHub OAuth другим кроком замінить лише endpoint логіну
- [x] Ліміт входу: 5 **невдалих** спроб за хвилину, окремо для кожної адреси-джерела.
      Керується вручну, а не middleware — той витрачав би спробу на кожен запит,
      включно з вдалим входом. Перевірка стоїть **до** звірки пароля, тож вичерпаний
      ліміт не дає підбирачу жодного зворотного зв'язку. 429 несе текст причини в тілі
- [x] `POST /api/auth/login`, `POST /api/auth/logout`,
      `GET /api/auth/me`; `/api/notes/upload` під `.RequireAuthorization()`
- [x] `OnRedirectToLogin` → 401 замість 302: дефолт cookie-схеми редіректить на сторінку
      логіну, і `fetch()` прочитав би HTML як успіх
- [x] Хеш пароля — PBKDF2 через `PasswordHasher<T>` (є у фреймворку, без NuGet).
      Генерація: `dotnet run -- hash-password <пароль>`, зберігання — user-secrets
- [x] Форма логіну на фронтенді, кнопка Sign out, перевірка сесії на старті
- [x] Прибрано `/weatherforecast`
- [x] Перевірено вручну (curl через Vite proxy): без cookie `me` і `upload` → 401;
      невірний пароль → 401; вірний → 200 + `Set-Cookie ... samesite=lax; httponly`;
      з cookie `me` і `upload` → 200; logout → 204, далі `me` → 401.
      Ліміт: 4 невдалі → вдалий вхід (200, спробу не витратив) → 5-та невдала 401 →
      6-та 429; вірний пароль при вичерпаному ліміті теж 429
- [x] `ForwardedHeaders` потрібен ще й для ліміту: за проксі всі запити приходять
      з однієї адреси, і поділ по клієнтах перестає працювати. Підключено;
      перевірено на зібраному Production-білді — 5 невдалих спроб із
      `X-Forwarded-For: 198.51.100.1` → 6-та 429, а перша спроба з `198.51.100.2` → 401

- [ ] **Повернути хешування пароля перед деплоєм.** Рішення від 2026-09-06: на час
      локальної розробки пароль лежить відкритим дефолтом `Password` у
      `Options/AuthOptions.cs` (був `OwnerPassword` у `Program.cs`; переїхав при
      рефакторингу структури). Отже він у git і залишиться в історії — цей самий рядок у прод
      їхати не може. Повернути треба: `PasswordHasher<T>` (PBKDF2), хеш у конфізі
      (`Auth:PasswordHash`), CLI-команду `dotnet run -- hash-password` для генерації,
      і порівняння в постійному часі замість `string.Equals`.
      У user-secrets хеш від `dev-local-password` лишився — не використовується.
      **Уточнення від 2026-09-06 (пізніше того ж дня): свідомо відкладено,** деплой їде
      з паролем у коді. Репозиторій публічний, тож публічний URL означає відкритий
      доступ до нотаток для будь-кого, хто зазирне в історію git. Лишається блокером
- [ ] Автотести замість ручної перевірки: xUnit + `WebApplicationFactory`
- [ ] 401 під час upload не скидає фронт у стан «не залогінений» — сесія протухла,
      а користувач бачить помилку завантаження. Потрібен спільний обробник 401.
      Після банера про зв'язок стало помітніше: протухла сесія рве і `GET /api/events`
      (401 → `CLOSED`), і банер каже «немає зв'язку з сервером», хоча сервер живий
      і просто не впізнає нас
- [ ] **Google OAuth як друге джерело identity** — за флагом `Features:GoogleSignInEnabled`
      (дефолт `false`). Пароль лишається, Google лише додається: обробник має
      `SignInScheme = cookie`, тож видає ту саму `kb.auth`, і `/api/auth/me`, logout
      та `[Authorize]` не змінились. Пакет `Microsoft.AspNetCore.Authentication.Google`
  - [x] Схема реєструється **тільки** за наявності `ClientId`/`ClientSecret`: обробник
        перевіряє свій callback на кожному запиті, а валідатор OAuth-опцій падає на
        порожньому `ClientId` — незаповнена схема поклала б увесь API, не лише вхід
  - [x] `CallbackPath = /api/auth/google/callback` замість дефолтного `/signin-google`:
        Vite проксює лише `/api`, і дефолтний шлях впав би у фронтенд
  - [x] Correlation-cookie: `SameSite = Lax` **і** `SecurePolicy = SameAsRequest`. Дефолти
        `None` + `Secure` разом вимагають https, і флоу ламався б на plain http — на
        localhost Chrome ще пропускає (secure context), а з телефона по LAN-IP уже ні.
        Google повертає top-level навігацією, тож `Lax` cookie доносить
  - [x] Allow-list `Auth:Google:AllowedEmails` — порожній означає **нікого не пускати**:
        сам вхід через Google вдається будь-якому акаунту у світі. Звірка в
        `OnTicketReceived` (не `OnCreatingTicket` — там `Fail()` ігнорується), відмова
        через `HandleResponse()` + редірект на `/?authError=…`, бо `Fail()` дав би 500
  - [x] `/api/features` віддає **ефективний** стан (`флаг && IsConfigured`), а
        `GET /api/auth/google/start` перевіряє флаг сам — щоб флаг не був лише декорацією
  - [x] Кнопка «Continue with Google» у формі логіну за флагом; це `<a>`, а не `fetch`:
        OAuth — ланцюг top-level навігацій, fetch крізь сторінки Google не проведе
  - [x] `changeOrigin: false` у Vite-проксі. Vite 8 за замовчуванням підміняє `Host` на
        адресу цілі, а бекенд будує `redirect_uri` саме з нього — Google повертав би
        браузер на `:5244`, повз проксі, на порт, який у dev не віддає SPA.
        Знайдено перевіркою: у `Location` стояв `redirect_uri=...localhost%3A5244...`
  - [x] Перевірено curl'ом через проксі: `/api/features` → `googleSignInEnabled: true`;
        `/api/auth/google/start` → 302 на `accounts.google.com` з
        `redirect_uri=http://localhost:5173/api/auth/google/callback` і cookie
        `samesite=lax; httponly` **без** `secure`; callback без correlation-cookie →
        302 на `/?authError=google-failed` (а не 500). З флагом `false`:
        `/api/features` → `googleSignInEnabled: false`, `google/start` → 404
  - [x] Реальний вхід через Google пройдено в браузері: згода Google → повернення на `/`
        → сесія є. Лишились неперевіреними лише негативні гілки з чужим акаунтом
        (`authError=google-not-allowed`)
  - [x] **Увімкнено в проді.** Окремий OAuth-клієнт (дев-ний у прод не їде), redirect URI
        `https://knowledgebase-9z29.onrender.com/api/auth/google/callback`, креденшели
        і `Features__GoogleSignInEnabled` — змінними оточення Render. Код виїхав
        окремо від увімкнення: у `appsettings.json` флаг лишається `false`, тож деплой
        сам по собі нічого не відкриває — рівно те, заради чого флаг і робився
  - [x] Перевірено на проді curl'ом: `/api/features` → `googleSignInEnabled: true`
        (ефективний стан, тобто креденшели теж на місці); `google/start` → 302 з
        **`redirect_uri=https://…`** і correlation-cookie `secure; samesite=lax`;
        callback без cookie → 302 на `/?authError=google-failed`.
        `https` тут і був головний ризик: Render віддає в контейнер plain http, і без
        `UseForwardedHeaders()` Google відповів би `redirect_uri_mismatch`. Той самий
        `SecurePolicy = SameAsRequest`, що локально прибрав `secure`, у проді його
        повернув — одна настройка, правильна поведінка в обох середовищах
  - [x] Реальний клік у проді пройдено. Google не показав ні вибору акаунта, ні згоди —
        браузер уже залогінений і дозвіл цьому клієнту вже виданий, тож підтвердження
        особи мовчазне і виглядає як «просто перекинуло на головну». Що вхід усе-таки
        стався, видно в DevTools: після `me` фронтенд тягне `assets` і `events`, а їх
        він запитує лише у стані `authenticated`
  - [ ] Показувати вибір акаунта щоразу — `GoogleChallengeProperties { Prompt =
        "select_account" }` замість `AuthenticationProperties`. Не робимо: для
        однокористувацького застосунку мовчазний вхід зручніший. Пункт на випадок,
        якщо з'явиться другий Google-акаунт і почнеться плутанина
- [ ] GitHub OAuth як друге джерело identity (allow-list на один акаунт) — після Google
      радше зайвий; лишається як варіант, а не план
- [ ] Антифоргері: `.DisableAntiforgery()` лишається на upload. `SameSite=Lax` не пускає
      auth-cookie на cross-site POST, тож зараз дірки немає — переглянути, якщо фронт
      і API колись роз'їдуться на різні адреси

## Інфраструктура і деплой

Схема (рішення від 2026-09-06): бекенд у хмарі — **stateless**, байти файлів
лишаються фізично на домашньому ПК, браузер ходить за ними напряму.

```
Браузер ──статика──► Cloudflare Pages
        ──API───────► Render (ASP.NET у Docker)
        ──байти─────► домашній ПК: Garage (S3) за тунелем
```

### Файловий сервер удома

- [x] Підняти **Garage** у Docker (S3-сумісний, Rust, під self-host).
      Не MinIO: community-репозиторій архівовано в лютому 2026 — без патчів безпеки.
      `infra/garage/`: compose з образом `dxflrs/garage:v2.3.0`, `garage.toml`,
      секрети в `.env` поза git, bootstrap-команди в `README.md`.
      Порти 3900/3903 прив'язані до `127.0.0.1` — назовні нічого не стирчить,
      поки перед Garage не поставлено тунель. Бакет `knowledgebase-assets`,
      ключ `knowledgebase-backend` з правами RW.
      `rpc_secret`/`admin_token` навмисне в env, а не у файлі конфігу: bind-mount
      на Windows виглядає для Garage world-readable, і він відмовляється стартувати.
      `root_domain` у `[s3_api]` не заданий — vhost-style вимагає wildcard-DNS,
      якого жоден тунель не дає, отже лишається тільки path-style.
      Перевірено round-trip'ом через одноразовий `amazon/aws-cli`: put → list → get,
      файл побайтово той самий
- [x] Дані — у volume на диску, щоб переживали перезапуск контейнера. Перевірено
      сильнішим тестом, ніж restart: `docker compose down` + `up` (контейнер
      створюється наново) — об'єкт на місці
- [x] Тунель для публічного HTTPS: **Tailscale Funnel** (`*.ts.net`, домен не потрібен).
      Cloudflare Tunnel відкинуто: вимагає власного домену на їхніх NS і має ліміт тіла
      запиту 100 МБ на Free. `tailscale funnel --bg 3900` →
      `https://rospc.tail11818d.ts.net` (443) → `127.0.0.1:3900`. Прив'язку Garage до
      loopback не чіпали — Funnel ходить із того самого хоста.
      Дозвіл вмикається атрибутом вузла (`nodeAttrs` + `funnel`) у політиці tailnet:
      візуальний редактор правил його не показує, тільки JSON editor. Плюс
      HTTPS Certificates на сторінці DNS.
      Перевірено ззовні: без ключів → 403 (відповідає Garage, а не тунель); put/list з
      ключами → працює; **presigned GET → 200 і побайтово той самий файл**, тобто Funnel
      не підміняє `Host`, на якому тримається підпис SigV4.
      Хостнейм публічний і знаходиться через Certificate Transparency — захист сховища
      тримається виключно на ключах
- [x] Окремий ключ для прода — `knowledgebase-render` (RW на бакет), щоб у хмарі був не
      той самий ключ, що в локальному `appsettings.Local.json`: відкликаються нарізно
- [x] Ключі доступу (access key / secret) — у змінних оточення Render
      (`Storage__Provider=S3`, `Storage__S3__*`). Сховище на проді працює — за словами
      користувача (2026-09-07): upload на `knowledgebase-9z29.onrender.com` доходить
      до Garage. Тобто `S3AssetStorage` підтверджено не лише проти локального Garage,
      а й у зв'язці Render → Tailscale Funnel → домашній ПК
- [x] **CORS на самому бакеті** — окремий конфіг Garage, не `AddCors` у ASP.NET.
      Браузер вантажить на інший origin, і дозвіл видає сховище.
      Правило в `infra/garage/cors.json`: тільки `PUT`, тільки з `http://localhost:5173`,
      тільки заголовок `content-type`. Ставиться через S3 API (`PutBucketCors`) — окремої
      команди в `garage bucket` немає. **Пастка:** RW-ключа замало, зміна конфігурації
      бакета вимагає прав owner, інакше `AccessDenied: Operation is not allowed for this
      key`. Owner видано на час правки і одразу знято — бекенду він у роботі не потрібен.
      Перевірено з реальної сторінки: `fetch` PUT з `http://localhost:5173` → 200,
      **той самий підписаний URL** з `http://localhost:5244` → `TypeError: Failed to
      fetch`, тобто блокує саме CORS, а не підпис. Preflight `OPTIONS` окремо: з дозволеного
      origin → 200 з `access-control-allow-methods: PUT`, з чужого → 403
- [ ] Фронт має пережити недоступність сховища: ПК вимкнено → API живий і віддає
      списки, а байти не тягнуться

### Абстракція сховища

- [x] `IAssetStorage` + `LocalFileAssetStorage` — інтерфейс і локальна реалізація за ним,
      зареєстровані в DI (singleton: теку створює конструктор, щоб паралельні перші
      завантаження не гонились). Ендпоінт більше не знає про файлову систему — віддає
      `Stream` і отримує `StoredAsset`. Перевірено реальним завантаженням: 200 і файл
      під GUID у `data/assets`
- [x] `S3AssetStorage` (`AWSSDK.S3` 4.0.102.5) другою реалізацією; перемикання —
      `Storage:Provider` = `Local` | `S3`, одним `if` у `StorageRegistration`.
      Ключі — лише через змінні оточення / user-secrets (`Storage__S3__AccessKeyId`);
      неповний конфіг валить застосунок на старті (`ValidateOnStart`), а не на першому
      завантаженні у вигляді 500. Іменування файлу винесено в `AssetFileName`, щоб
      `SafeExtension` не дублювався у двох реалізаціях.
      **Пастка AWS SDK v4:** дефолт `RequestChecksumCalculation = WHEN_SUPPORTED`
      перетворює PUT на chunked-тіло з трейлером контрольної суми, і Garage відповідає
      `Invalid payload signature`. Виставлено `WHEN_REQUIRED` у конфізі клієнта.
      Перевірено проти локального Garage: логін → upload → 200, об'єкт у бакеті
      побайтово збігається з надісланим; порожній файл → 400, без cookie → 401;
      `Provider=Local` поводиться як раніше
- [x] Локальні налаштування — `backend/appsettings.Local.json` (`UseLocalOverrides`):
      у `.gitignore` і `.dockerignore`, підключається **лише в Development**. Джерело
      додається після `CreateBuilder`, тому перебиває всі інші, включно зі змінними
      оточення — гейт на Development саме для цього: копія, що осіла поруч із
      бінарником, не перебʼє конфіг Render. Перевірено: ключі Garage лежать у файлі,
      `Storage__Provider=S3 dotnet run` вантажить у бакет без жодної змінної з ключами;
      без цієї змінної файли лягають у `data/assets`, як раніше; той самий файл поруч
      із бінарником у Production ігнорується (падіння на `ValidateOnStart`);
      `docker build` не бачить його в контексті (перевірено пробним образом)
- [x] **Postgres + EF Core під метадані файлів.** Новий зріз `Infrastructure/Persistence/`:
      `AssetRecord`, `KnowledgeBaseDbContext`, `AddDatabase`/`MigrateDatabase`; рядок
      підключення — `ConnectionStrings:Database`, міграції накатуються на старті.
      Спершу був SQLite (`data/app.db`) — відпав, щойно з'ясувалось, що на free-плані
      Render контейнер щодеплою чистий і диска під файл бази там немає взагалі.
      Локально Postgres у Docker (`infra/postgres`, той самий рушій, що в проді),
      у хмарі — Neon. EF Core, а не Dapper — заради міграцій.
      **Таблиця стала джерелом правди**: `GET /api/assets` читає її, а не сховище;
      `IAssetStorage` лишився відповідальним тільки за байти. Порядок операцій обрано
      під відсутність спільної транзакції: upload — спершу байти, потім рядок
      (сирота-файл невидимий, але нешкідливий), delete — спершу рядок, потім байти.
      `SaveChangesAsync` навмисно без `cancellationToken`: клієнт, що відвалився, не має
      коштувати рядка, без якого вже збережені байти стають невидимими.
      Перевірено curl'ом проти локального Postgres: upload → рядок у таблиці
      (`psql`: `Assets` + `__EFMigrationsHistory`, `UploadedAtUtc` типу
      `timestamp with time zone`), список з `originalFileName`, скачування під
      оригінальним іменем, `DELETE` → 204 і зникнення файлу з диска, повторний → 404.
      Раніше те саме пройдено на SQLite, разом із виживанням рядків після рестарту.
      **Пастка SQLite, через яку і зʼявився обхід:** він не сортує `DateTimeOffset`
      у `ORDER BY`, а `DateTime` віддає з `Kind.Unspecified` — тобто без `Z` у JSON,
      і браузер читав би UTC як локальний час. Лікувалось конвертером у моделі;
      у Postgres не потрібне взагалі — там `timestamp with time zone`
- [x] Рядок збирає фабрика `AssetRecord.For` — приймає окремі значення, а не `IFormFile`:
      записи скоро створюватиме AI-пайплайн, куди HTTP-запит не приходить. Порожнє ім'я
      підміняється збереженим, порожній MIME — `application/octet-stream`. Перевірено
      завантаженням файлу без розширення і без Content-Type: рядок і скачування коректні
- [x] Оригінальне ім'я і MIME-тип тепер зберігаються — закрилось усе, що з цього тягнулось:
      список показує «my report.pdf», `Content-Disposition` віддає файл під тим самим
      іменем (`filename*=UTF-8''my%20report.pdf`), `confirm` перед видаленням питає про
      впізнаваний файл. `Content-Type` при віддачі береться з рядка, а не вгадується з
      розширення — `FileExtensionContentTypeProvider` прибрано з контролера
- [ ] `GET /api/assets/{fileName}` при `Features:DownloadEnabled=false` віддає **406**,
      а не 403: на дії стоїть `[Produces("application/octet-stream")]`, а гілка з
      вимкненим прапорцем повертає JSON-обʼєкт, і узгодження типів провалюється.
      Спливло під час перевірки R2 — довелось вмикати прапорець змінною, щоб узагалі
      дійти до байтів
- [x] Content-Type самого об'єкта закрито разом із прямим доступом: у підписаному PUT
      тип **входить у підпис**, тож браузер зобов'язаний надіслати саме його, і об'єкт лягає
      в бакет уже з правильним типом. Для об'єктів, залитих раніше, тип і ім'я підставляються
      в момент видачі посилання через `ResponseHeaderOverrides`, тобто джерелом лишається
      таблиця. `IAssetStorage.SaveAsync` (старий шлях через бекенд) як не приймав тип, так і
      не приймає — на нього це вже не впливає
- [x] **Прод без стану — зроблено.** Контейнер на Render більше не зберігає нічого:
      метадані в Neon, ключі Data Protection там само, байти в R2. Перевірено на живій
      адресі 2026-09-07: логін → `200`, upload → `201`, рядок у `Assets` у Neon,
      об'єкт у бакеті R2 **побайтово** той самий (звірено читанням напряму з бакета,
      повз застосунок), `DELETE` → `204` і зникнення і рядка, і байтів, повторний
      `DELETE` → `404`. Скачування через API теж прогнане: `200`,
      `Content-Disposition` з оригінальним іменем, файл побайтово збігається з
      надісланим — але лише після того, як на Render увімкнули
      `Features__DownloadEnabled=true`. Міграції накотились самі на старті: у Neon
      зʼявились `Assets`, `DataProtectionKeys` і `__EFMigrationsHistory` з обома записами.
      **Зміна змінної оточення сама сервіс не перезапускає** — попри те, що деплой
      після неї в логах з'являється, нове значення підхоплюється лише після
      `Manual Deploy → Deploy latest commit`. Перевірено: до ручного деплою скачування
      і далі віддавало `406`.
      **Ліміти, з якими живемо** (перевірено 2026-09-07): Neon Free — 0.5 ГБ на проєкт,
      100 CU-годин і 5 ГБ трафіку на місяць, сон після 5 хв простою з прокиданням за
      сотні мілісекунд; R2 — 10 ГБ, 1 млн/10 млн операцій, вихідний трафік безкоштовний,
      але **вимагає прив'язати платіжку**; Render Free — сон після 15 хв без трафіку,
      прокидання **близько хвилини**, 750 інстанс-годин на місяць на весь workspace
      (тобто прод і staging безкоштовно не поєднати)
- [x] **Проєкт у Neon заведено і прив'язаний до репо** — перший з трьох кроків
      «прода без стану» вище. `neon link` створив `.neon` (org, project
      `hidden-thunder-38922758`, гілка `production`) і витяг `DATABASE_URL`,
      `DATABASE_URL_UNPOOLED` і `NEON_BRANCH` у `.env.local`. Обидва файли — в
      кореневому `.gitignore`, разом із `node_modules/`, який притягнув `neon config init`.
      `neon.ts` тримає політику гілки як код; зараз там порожній `defineConfig({})`,
      тобто свідомо без відхилень від дефолтів проєкту. Перевірено: `neon config plan`
      і `neon deploy` проти `production` → `No changes — branch production already
      matches the policy`
- [x] Інструментарій агента лишається в межах цього репо, а не машини: 7 скілів Neon
      у `.claude/skills/` (+ `skills-lock.json`), MCP-сервер — у **local**-скоупі
      (`~/.claude.json` під ключем цієї теки), а не в user-скоупі, куди його поклав
      `neon mcp`: з user-скоупу він видний у кожному проєкті на машині. Записи, які та
      ж команда розклала по конфігах Copilot, opencode і VS Code, прибрано.
      Акаунтний API-ключ, який вона намінтила, **відкликано** (`neon api-keys revoke
      3319320`) — він дотягувався до всіх організацій і лежав відкритим текстом одразу
      в чотирьох конфігах. Замість нього ключ, обмежений одним проєктом
      (`neon api-keys create --project-id`, id 3319419): він не вміє ні створювати
      проєкти, ні мінтити ключі, ні читати чужі — тільки цей. Авторизація саме токеном,
      а не OAuth: OAuth-флоу тут не спрацював. Ключ лежить у local-скоупі
      `~/.claude.json`, який у git не потрапляє
- [x] Рядок підключення заведено. Пастка на цьому шляху виявилась подвійна.
      **Перша:** Npgsql не розуміє URL-формат, у якому Neon віддає `DATABASE_URL`
      (`postgresql://user:pass@host/db`) — ADO.NET хоче `ключ=значення`. Перевірено
      окремою пробою: `NpgsqlConnectionStringBuilder` на такому рядку кидає
      `ArgumentException`. Перекладено руками у
      `Host=...-pooler...;Database=neondb;Username=...;Password=...;SSL Mode=Require`;
      `channel_binding=require` з URL викинуто — такої опції в Npgsql немає, SCRAM
      channel binding вмикається сам. **Друга:** масова вставка `Add from .env` у Render
      ріже рядок по першому `=`, а тут `=` є всередині значення — у панелі виникла
      змінна з ключем `Host` замість `ConnectionStrings__Database`, і застосунок упав
      на старті. Рядки з `=` у значенні додавати тільки по одному, руками.
      Локально це й далі `appsettings.Development.json` з Postgres у Docker: про
      `.env`-файли бекенд не знає взагалі, він читає `ConnectionStrings:Database`
      з конфігу ASP.NET
- [ ] **Збір сиріт**: звірка таблиці зі сховищем. Байти без рядка (upload упав посередині,
      файл поклали в теку повз застосунок) API не бачить взагалі. Саме для цього лишився
      `IAssetStorage.ListAsync` — контролер його вже не викликає
- [ ] Два `.jpg`, завантажені до появи таблиці, зі списку зникли: рядків для них немає.
      Свідомо не засівав — це дебажні файли. Приберуться разом зі збором сиріт
- [x] `IAssetStorage` уміє не тільки зберігати: додано `ListAsync`, `OpenReadAsync`
      (повертає `null`, якщо нема) і `DeleteAsync` (`false`, якщо нема) в обох
      реалізаціях. У S3 список гортає сторінки по `IsTruncated`/`ContinuationToken` —
      без цього видно тільки перші 1000 ключів; `DELETE` на S3 віддає `204` і для
      неіснуючого ключа, тому перед ним стоїть `GetObjectMetadataAsync`,
      інакше `404` був би неможливий
- [x] Прибрати захардкоджений `ContentRootPath` з `Program.cs` — шлях тепер із конфігу
      (`Storage:AssetsPath`, дефолт `data/assets`) і резолвиться всередині
      `LocalFileAssetStorage`; абсолютний шлях у конфізі береться як є. S3-реалізації
      `ContentRootPath` не дістанеться взагалі
- [x] **Байти більше не йдуть через бекенд.** Render (і локальний API) тепер лише підписує
      посилання, а браузер ходить у бакет сам — та сама схема, що намальована на початку
      розділу «Інфраструктура і деплой», доведена до коду.
      Три нові дії в `AssetsController`: `GET /api/assets/{name}/link` (підпис на скачування),
      `POST /api/assets/upload-link` (підпис на запис + резервування ключа),
      `POST /api/assets/{name}/confirm` (рядок у таблиці після того, як байти вже в бакеті).
      Старі `POST /api/assets` і `GET /api/assets/{name}` лишились — вони під прапорцем
      вимкненого прямого доступу і далі працюють як раніше.
      **Підпис винесено в окремий `IAssetLinkSigner`, а не в `IAssetStorage`:** підписувати
      вміє тільки бакет, і локальна реалізація мусила б кидати «не вмію». Замість цього
      інтерфейс реєструється **лише** на гілці `Provider=S3`, а в контролери інжектиться
      як `IAssetLinkSigner? signer = null` — DI підставляє дефолт, коли сервісу немає.
      Прапорець `Features:DirectAssetAccessEnabled` (дефолт `false`), `/api/features` віддає
      **ефективний** стан (`прапорець && є чим підписати`). Суперечливий конфіг
      (`Provider=Local` + прапорець) валить застосунок **на старті**, а не на першому кліку.
      Порядок в upload'і той самий, що й був: спершу байти, потім рядок. Тільки тепер між
      ними — інший процес, тож незакритий `confirm` лишає **сироту**: об'єкт, якого не видно
      в API взагалі
- [x] Ліміт розміру пережив переїзд, але ослаб і це свідомо: підписаний PUT неможливо
      обмежити, тож `upload-link` перевіряє **заявлений** розмір (дешева рання відмова), а
      справжня перевірка — у `confirm`, по `HeadObject`. Завеликий об'єкт видаляється, рядок
      не створюється. Перевірено брехнею: посилання випрошене під 1000 байт, залито 27 МБ →
      `confirm` `400` і об'єкт зник із бакета (повторний `confirm` → `404`)
- [x] Повторний `confirm` того самого ключа → `409`, щоб загублена відповідь не породила
      два рядки на один об'єкт
- [x] **Пастка AWS SDK, третя за ліком:** `GetPreSignedUrlRequest.Protocol` за замовчуванням
      `HTTPS` і **ігнорує схему з `ServiceUrl`**. Локальний Garage слухає plain http, і перший
      же підписаний URL вів на `https://localhost:3900` — порт, який TLS не розуміє, тож
      `curl` віддавав `HTTP 000` ще до будь-якого підпису. Протокол тепер виводиться зі схеми
      `ServiceUrl`. У проді (R2 за https) це не спливло б узагалі — класичний баг, який ловиться
      тільки локально
- [x] `GET /api/assets/{name}/link` перед підписом перевіряє, що об'єкт узагалі є
      (`HeadObject`). Без цього рядок-сирота (є в таблиці, немає в бакеті) отримував робоче
      посилання, бакет відповідав XML-помилкою, і **сторінка застосунку замінювалась на цей
      XML** — навігація ж top-level. Знайдено перевіркою в браузері на реальному такому рядку
- [x] Скачування на фронті — прихований `<iframe>`, а не `window.location.assign`:
      відповідь-attachment качається однаково, а будь-яка інша (протухле посилання, зниклий
      об'єкт) у фреймі просто відкидається замість того, щоб винести користувача зі SPA
- [x] Перевірено наскрізь `curl`'ом проти локального Garage: `upload-link` → `PUT` у бакет
      (`200`) → рядка ще немає в списку → `confirm` (`201`) → рядок у списку з оригінальним
      іменем і розміром 200 000 → повторний `confirm` (`409`) → `link` (`200`) → `GET` з
      бакета (`200`, `content-disposition: attachment; filename="my report.pdf";
      filename*=UTF-8''my%20report.pdf`, `content-type: application/pdf`, байти **побайтово**
      ті самі) → `DELETE` (`204`).
      Негативні гілки: заявлений розмір понад ліміт → `400`, нульовий → `400`, порожнє тіло
      через валідне посилання → `confirm` `400`, зіпсований на один символ підпис → `403`,
      правильний підпис але інший `Content-Type` → `403` (тип входить у підпис),
      посилання на неіснуюче ім'я → `404`.
      З вимкненим прапорцем усі три нові дії → `403`, а старий шлях працює як раніше
      (multipart upload `201` → скачування `200`, байти ті самі → `DELETE` `204`).
      `Provider=Local` + прапорець → застосунок не стартує:
      `InvalidOperationException: Features:DirectAssetAccessEnabled needs Storage:Provider=S3`
- [x] Скачування перевірено кліком у браузері: ім'я файлу — кнопка, клік дає
      `GET /api/assets/{name}/link` (`200`) і одразу `GET localhost:3900/...` з
      `X-Amz-Signature` і `response-content-disposition` (`200`); вкладка лишається на
      `localhost:5173`, CORS-помилок у консолі немає
- [ ] **Прямий upload кліком у браузері досі не пройдено.** Автоматизація не вміє заповнити
      `<input type="file">` (`form_input` падає на `HTMLInputElement.value`), а обхід через
      `DataTransfer` перевіряв би не той шлях. Сам CORS-preflight і `PUT` з origin сторінки
      доведені окремо (`fetch` з консолі), контракт — `curl`'ом, але «вибрав файл → рядок
      з'явився» лишається за живою людиною. Один клік
- [ ] Сироти: об'єкт, залитий у бакет, чий `confirm` не дійшов, лишається в сховищі назавжди
      і невидимий для API. Потрібне прибирання — lifecycle-правило на бакеті або команда,
      що звіряє список об'єктів із таблицею. З'явилось разом із прямим upload'ом, раніше
      такого стану не існувало
- [ ] CORS на R2 і вмикання `Features__DirectAssetAccessEnabled` у проді. Код їде з
      прапорцем `false`, тож деплой сам по собі нічого не змінює — те саме, що робили
      з Google-входом
- [ ] Недоступне сховище тепер видно і на скачуванні: `/link` робить `HeadObject`, і при
      вимкненому ПК це буде `500`, а не зрозуміла відмова. Той самий пункт, що вже стоїть
      вище («фронт має пережити недоступність сховища»), просто з новим місцем прояву


- [x] Перехід на R2 — **одна правка коду, а не нуль**, як очікувалось. Конфіг справді
      змінився лише в `ServiceUrl`, `BucketName`, `Region` (`auto`) і ключах, але upload
      падав `500`: `STREAMING-AWS4-HMAC-SHA256-PAYLOAD not implemented`. AWS SDK ріже тіло
      на шматки й підписує кожен окремо — R2 такого не вміє. Це **не** та сама пастка, що
      з Garage: `RequestChecksumCalculation = WHEN_REQUIRED` закриває CRC32-трейлер і
      лишається на місці, а chunked-підпис вимикається окремо. Важелі є тільки на запиті,
      не в конфізі клієнта (перевірено рефлексією по `AWSSDK.S3` 4.0.102.5:
      `UseChunkEncoding`, `DisablePayloadSigning`), тому в `PutObjectRequest` доданий
      `UseChunkEncoding = false` — один підпис по всьому тілу.
      Garage від цього не постраждав: повний цикл проти нього прогнаний після правки,
      скачаний файл побайтово збігається з надісланим, `DELETE` → `204`, повторний → `404`
- [ ] R2 налаштований лише як сховище прода. Garage лишається локальним провайдером
      (профіль `http-s3`) і другою копією; переносу вже наявних обʼєктів між ними немає
      і поки не потрібно

### Бекенд у хмарі

- [x] Multi-stage `Dockerfile` у корені репо: `node:24-alpine` збирає фронт,
      `sdk:8.0-alpine` — бекенд, `aspnet:8.0-alpine` виконує; `dist` лягає у `wwwroot`.
      Render не має нативного .NET-рантайму — тільки Docker.
      Зібрано локально (`docker build -t knowledgebase:local .`) — 163 МБ, без помилок;
      контейнер запущено як на Render (`-e PORT=10000`) і прогнано весь набір перевірок:
      `/` і `/notes` → 200 `text/html`, `/assets/*.js` → 200 `text/javascript`,
      `/health` → 200, `/api/does-not-exist` → 404, логін/`me`/upload → 200,
      без cookie → 401. `/swagger` у Production → 404, як і має бути
- [x] Kestrel слухає порт зі змінної `PORT` (Render передає свій). Перевірено:
      `PORT=8099 dotnet Backend.dll` → `Now listening on: http://[::]:8099`
- [x] `ForwardedHeaders` **перед** `UseHttpsRedirection` — за проксі інакше
      виходить цикл редіректів. Перевірено на Production-білді: запит із
      `X-Forwarded-Proto: https` віддає 200, без нього — 307 на `https://`.
      Побічно підтвердилось прапорцем `secure` в `Set-Cookie` (політика
      `SameAsRequest` бачить схему саме із заголовка)
- [x] **Ключі Data Protection — у базу.** Ними шифрується вміст auth-cookie, і за
      замовчуванням вони лежать на диску — а диск Render ефемерний, звідси розлогін
      щодеплою. Диск на Free недоступний, але база вже є: пакет
      `Microsoft.AspNetCore.DataProtection.EntityFrameworkCore`,
      `KnowledgeBaseDbContext : IDataProtectionKeyContext`, реєстрація
      `PersistKeysToDbContext` живе в `AddAuthFeature` — там же, де налаштовується cookie.
      Таблиця `DataProtectionKeys` приїжджає міграцією `AddDataProtectionKeys`.
      **Перевірка рестартом тут нічого не доводить:** на Windows ключі й до змін лежали
      в профілі користувача (`%LOCALAPPDATA%\ASP.NET\DataProtection-Keys`), тож сесія
      переживала перезапуск і раніше. Доказ інший: після логіну в таблиці зʼявився рядок
      з `<key id=...>`; після `DELETE FROM "DataProtectionKeys"` і рестарту **та сама
      cookie дає 401**, а новий логін — 200. Отже ключ береться саме з бази.
      Симптом до виправлення був підтверджений на локальному контейнері: ASP.NET сам
      писав у лог `Storing keys in a directory '/root/.aspnet/DataProtection-Keys' that
      may not be persisted outside of the container`, і на **новому** контейнері стара
      cookie давала 401
- [x] Віддача зібраного фронту з ASP.NET (`UseStaticFiles` + fallback на `index.html`) —
      тепер **передумова авторизації**, а не косметика: одна адреса означає, що cookie
      одразу налаштовуються в остаточному вигляді. CORS у проді не потрібен узагалі.
      Перевірено: `/` і глибоке посилання `/notes` → 200 `text/html`, `/assets/*.js` → 200.
      Фолбек навмисне подвійний: `MapFallback("/api/{**path}")` віддає 404, бо
      суцільний catch-all відповідав би на друкарську помилку в шляху API сторінкою
      `index.html` зі статусом 200, і `fetch()` прочитав би розмітку як успіх
- [ ] Якщо колись фронт і API роз'їдуться на різні адреси — список дозволених origin
      брати з конфігу, не хардкодити (актуально лише для того сценарію)
- [x] Створити сам Web Service на Render: runtime Docker, `Dockerfile` у корені,
      гілка `main`. Задеплоєно: <https://knowledgebase-9z29.onrender.com/>.
      Перевірено на живій адресі: `/` і `/notes` → 200 `text/html`, `/assets/*.js` → 200,
      `/health` → 200 `Healthy`, `/swagger` → 404, `/api/nope` → 404,
      `me` без cookie → 401, невірний пароль → 401, вірний → 200 з
      `Set-Cookie ... secure; samesite=lax; httponly`, `me` з cookie → 200,
      logout → 204. `http://` → 301 на `https://` віддає edge Render, ще до застосунку
- [ ] Врахувати, що Free-інстанс **засинає після 15 хв без трафіку**, підйом ~1 хв.
      Перший запит після паузи виглядатиме як зависання
- [x] Ефемерна файлова система з'їдала й `data/assets`: файли зникали при кожному деплої.
      Знято переходом прода на `Storage:Provider=S3` — байти тепер у Garage, не на диску
      Render. Актуально лишається тільки для локального `Provider=Local`

### Пайплайни

- [x] CI для фронтенду (`.github/workflows/frontend-ci.yml`: `npm ci`, lint, build) —
      зелений на GitHub (2026-09-06, за словами користувача; `gh` на машині немає)
- [x] CI для бекенду (`.github/workflows/backend-ci.yml`: `dotnet restore` +
      `build -c Release`) — зелений там само
- [ ] Додати `dotnet test` у backend-CI, коли з'явиться перший тест-проєкт
- [ ] CD: `push у main → build + lint + test → зелено → curl Deploy Hook Render`.
      Саме так, а не Render auto-deploy: авто-деплой викотить і зламану збірку,
      бо про тести він нічого не знає
- [ ] Deploy Hook URL — у секретах репозиторію
- [ ] Окремий пайплайн для фронту на Cloudflare Pages (якщо не віддавати з ASP.NET)

## AI-пайплайн

- [ ] Абстракція клієнта моделі (інтерфейс + реалізація на `HttpClient`), щоб її можна
      було підмінити фейком у тестах — закласти **до** написання логіки
- [ ] Ключ моделі через user-secrets / змінні оточення
- [ ] Аналіз PDF та зображень → генерація `.md`
- [ ] Автовизначення категорії
- [ ] Двосторонні `[[wiki-links]]`: вставка backlinks у наявні нотатки
- [ ] Асинхронна обробка: `202 Accepted` + `jobId`, опитування статусу або SSE
      (синхронний запит на 30+ секунд не годиться)
- [ ] Ідемпотентність: повторна обробка не має дублювати посилання
- [ ] Обробка помилок моделі: битий вихід не повинен псувати наявні нотатки

## Фронтенд

- [x] **Падіння беку видно на сторінці.** Симптом один («сторінка показує актуальне, хоча API мертвий»), а місць три:
      1) `api/realtime.ts` тепер тримає стан з'єднання і роздає його через
      `subscribeToConnection`. `paused` — свідоме закриття (схована вкладка, простій
      15 хв, нема підписників), банер на нього не реагує; `offline` — будь-який
      `onerror`, **включно з тим, коли браузер ретраїть сам** (`readyState` =
      `CONNECTING`): для сторінки це така сама відсутність зв'язку. Заразом `sync()`
      закриває і відкладений реконект, інакше стрім, якого ніхто не хоче, лишався б
      у стані `offline`. Хук `useConnectionStatus` + банер в `App`.
      2) Холодний старт із мертвим API більше не кидає фронт у `anonymous` — це була
      брехня: показувалась форма входу, хоча входити нікуди. Новий стан `unreachable`
      з кнопкою «Try again». Ознака не «мережева помилка», а «`fetchCurrentUser`
      кинув»: `null` він віддає лише на 401, тож будь-який виняток означає «про сесію
      нічого не відомо».
      3) `apiFetch` в `api/http.ts` перекладає відмову `fetch` у `ApiUnreachableError`.
      `fetch` кидає лише коли відповіді не було взагалі, і текст у нього свій у кожному
      рушії — «Failed to fetch» перед користувачем бути не має.
      Перевірено в браузері, усі чотири сценарії: бек убито при відкритій сторінці —
      банер за ~3 с; бек піднято — банер зник сам, стрім перепідключився (у мережі видно
      4 × 502 через Vite-проксі, тобто спрацював саме шлях `CLOSED` + backoff, а не
      власний ретрай браузера); холодний старт із мертвим беком — екран
      `Could not read the session (HTTP 502)` з кнопкою, і «Try again» після підняття
      беку повернув сесію та список; схована вкладка — стрім закрився через 30 с
      (`ERR_ABORTED` у мережі), банер **не** з'явився, на поверненні стрім відкрився знову

- [ ] Прибрати невикористані файли шаблону: `src/assets/{react.svg,vite.svg,hero.png}`,
      `public/icons.svg` — закомічені, але жодного посилання на них у коді немає
- [x] Прибрано панель із сирим JSON після завантаження — залишок із часів, коли показати
      результат більше не було де. Тепер про успіх каже новий рядок у таблиці.
      Заразом зникла її гірша риса: панель не очищалась після видалення файлу і висіла
      з текстом «Stored X…» про те, чого вже нема. Стан `success` із `UploadState`
      прибрано разом із нею, лишились `idle`/`uploading`/`error`
- [x] Агент `mobile-layout-checker` (`.claude/agents/`) — субагент, який ганяє верстку
      на 375/320/768 у двох темах і міряє, а не оцінює на око: ширина документа проти
      ширини екрана, елементи за краєм, тап-таргети <44 px. Нічого не редагує, серверів
      не чіпає, нічого не видаляє. Визначення читається на старті сесії — у тій, де його
      створено, воно ще не видно
- [x] Верстка списку файлів на телефоні. Знайдено першим же прогоном агента: `nowrap`
      на комірках таблиці робив її мінімальну ширину 400 px, сторінка ставала 432 px
      при екрані 375 (на 320 — 434, тобто 36% поза екраном), **кнопки Delete повністю
      виїжджали за край** — на телефоні файл неможливо було видалити. Колонка з іменем
      при цьому схлопувалась до 47 px і `word-break: break-all` розсипав ім'я в стовпчик
      по 2–3 символи: посилання 16 px завширшки і 395 px заввишки.
      Таблицю замінено списком (`ul` + CSS Grid): на широкому екрані три колонки, як
      і була таблиця, на вузькому ім'я займає свій рядок, а розмір/дата й кнопка йдуть
      під ним. Таблиця тут узагалі була не за призначенням — це список файлів,
      а не табличні дані. Заразом кнопка Delete виросла до 44 px заввишки
- [x] `h1` на телефоні накладався сам на себе: `font-size: 36px` без власного
      `line-height`, а успадкований від `body` відсотковий перетворюється на **фіксовані
      23 px** (відсотки в `line-height` наслідуються вже обчисленими). Проявлялось лише
      коли кнопка «Sign out» стискала заголовок і той переносився. Задано `110%`
- [ ] Нативний `<input type="file">` — 21 px заввишки, вдвічі менше за орієнтир 44 px
      для пальця. Лікується лише схованим інпутом і власною кнопкою-лейблом
- [ ] Заборонити агенту верстки підставляти власну розмітку в сторінку: щоб не
      розлогінюватись, він виміряв екран входу на вставленій копії форми замість живого
      екрана. Класи й CSS ті самі, але це вже не той вимір
- [ ] Enter у полі пароля не сабмітить форму логіну — вхід тільки кліком по кнопці
- [ ] Дата у списку файлів — сирий `toLocaleString()` із секундами (`9/6/2026,
      9:52:14 PM`). Формат береться з локалі браузера, секунди тут ні до чого
- [ ] `GET /api/auth/me` віддає 401 до входу, і браузер пише червону помилку в консоль
      на кожному холодному старті. Поведінка правильна, шум — ні
- [ ] Переписати `frontend/README.md` — зараз там дефолтний текст шаблону Vite
- [ ] Після рестарту беку сторінка чекає на backoff: 502 через проксі кладе стрім у
      `CLOSED`, і затримка подвоюється 2→4→8→16→…→60 с. У прогоні банер провисів ~30 с
      **після** того, як API вже відповідав. Напрошується скидати затримку на дію
      користувача або на повернення вкладки — зараз `sync()` не відкриває стрім, поки
      висить `reopenTimer`
- [ ] Перевірити клієнтську частину SSE в браузері: закриття стріму при схованій вкладці,
      простій 15 хв, перезапит списку після повернення, перепідключення після рестарту API.
      Сервер і Vite-проксі перевірені `curl`'ом, це — ні
- [ ] `reloadToken` після власного upload лишився поряд зі стрімом: він оновлює список без
      чекання на round-trip і рятує, якщо стрім лежить. Ціна — два `GET` замість одного
      після свого ж завантаження. Прибрати, якщо стрім виявиться надійним
- [ ] Роутинг (react-router)
- [ ] Робота з серверним станом (розглянути TanStack Query)
- [ ] Markdown-редактор
- [ ] Пошук по нотатках
- [ ] Граф зв'язків

## Тести

- [ ] xUnit + `WebApplicationFactory` — інтеграційні тести API. Тепер є що підміняти:
      `IAssetStorage` замінюється фейком через `WithWebHostBuilder`, і тести не пишуть
      на диск. Замінює ручний прогін curl, який поки робиться щоразу вручну
- [ ] **Тести на backlinks** — найкритичніше: мутація багатьох файлів над накопиченими
      особистими даними, ламається тихо
- [ ] Vitest + Testing Library — стани форми завантаження
- [ ] Playwright — e2e-сценарій завантаження

## Експлуатація

- [ ] Стрімінг великих файлів замість буферизації (зараз ліміт 25 МБ перевіряється вже
      після того, як тіло запиту забуферене)
- [ ] Валідація типів файлів, що приймаються
- [ ] Бекап теки `data/` (і volume Garage після переїзду)
- [x] Health-check endpoint — Render пінгує його, щоб знати, що інстанс живий.
      `AddHealthChecks()` + `MapHealthChecks("/health")`, без авторизації.
      Перевірено: `GET /health` → 200 `Healthy` і в Production, і в Development
