.neon
.env.local
node_modules/

# Filled-in prod credentials for the home-PC worker.
infra/worker/worker.env

# Output of .claude/skills/generate-test-data.
/test-data/

# dotnet build outputs redirected away from bin/obj (running Api/Worker lock those).
.dotnet-artifacts/
